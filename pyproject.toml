[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "freeflow"
version = "0.3.1"
requires-python = ">=3.11"
dependencies = [
    "evdev",
]

[project.scripts]
freeflow = "freeflow.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/freeflow"]
